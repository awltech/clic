#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<site>
	<feature
		url="features/${groupId}.${rootArtifactId}.feature_${version}.qualifier.jar"
		id="${groupId}.${rootArtifactId}.feature" version="${version}.qualifier">
		<category name="${rootArtifactId}" />
	</feature>
	<category-def name="${rootArtifactId}" label="${rootArtifactId}">
		<description>
			${rootArtifactId} Clic plugin
		</description>
	</category-def>
</site>